<tal:r i18n:domain="bes.lims"
  define="report_id string:sample_type_contamination_rate_by_month">

  <h2 i18n:translate="">
    2.1 Contamination rate by month for each sample type
  </h2>

  <form method="post">

    <!-- Target patient control -->
    <div class="form-group form-inline">
      <tal:target_patient replace="structure python:view.target_patient_control()"/>
    </div>

    <!-- Year control -->
    <div class="form-group form-inline">
      <tal:year replace="structure python:view.year_control()"/>
    </div>

    <!-- Submit button -->
    <input tabindex=""
           class="searchButton allowMultiSubmit"
           type="submit"
           name="submit"
           value="Generate report"
           i18n:attributes="value"/>

    <!-- hidden fields -->
    <input tal:replace="structure context/@@authenticator/authenticator"/>
    <input type="hidden" name="report_id" tal:attributes="value report_id"/>

  </form>

</tal:r>
